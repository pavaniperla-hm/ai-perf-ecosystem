apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: perf-demo
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 80
      targetPort: 8003
