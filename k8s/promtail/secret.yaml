apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: perf-demo
type: Opaque
stringData:
  # Paste your Grafana API token here before applying.
  # Never commit a real token — keep this file gitignored or use a sealed secret.
  GRAFANA_API_TOKEN: "REPLACE_WITH_YOUR_GRAFANA_API_TOKEN"
  LOKI_USERNAME: "1494446"
  LOKI_URL: "https://logs-prod-025.grafana.net"
